<?xml version="1.0" encoding="UTF-8" ?>
<config>
  <!-- Aligned with term/property cores to avoid managed schema surprises -->
  <luceneMatchVersion>8.8.2</luceneMatchVersion>
  <dataDir>${solr.data.dir:}</dataDir>
  <directoryFactory name="DirectoryFactory" class="solr.NRTCachingDirectoryFactory"/>
  <codecFactory class="solr.SchemaCodecFactory"/>
  <!-- Use classic (static) schema to ensure our provided schema.xml is used exactly -->
  <schemaFactory class="ClassicIndexSchemaFactory"/>
  <updateHandler class="solr.DirectUpdateHandler2"/>
  <query>
    <useFilterForSortedQuery>true</useFilterForSortedQuery>
    <queryResultWindowSize>200</queryResultWindowSize>
    <queryResultMaxDocsCached>200</queryResultMaxDocsCached>
  </query>
  <requestHandler name="/select" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="defType">edismax</str>
      <str name="echoParams">explicit</str>
      <str name="qf">lemmaExact^100 lemma^50 writtenRepExact^80 writtenRep^40 conceptLabel^10 definition^5</str>
      <str name="hl">on</str>
      <str name="hl.fl">lemmaExact lemma writtenRepExact writtenRep conceptLabel definition</str>
      <str name="fl">*,score</str>
      <int name="rows">50</int>
    </lst>
  </requestHandler>
  <requestHandler name="/update" class="solr.UpdateRequestHandler"/>
</config>
